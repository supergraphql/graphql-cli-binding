{
  "name": "graphql-cli-bundle",
  "version": "0.1.2",
  "description": "Processes import statements in a GraphQL schema and outputs a single schema file",
  "main": "dist/index.js",
  "typings": "dist/index.d.ts",
  "scripts": {
    "prepublish": "npm run build",
    "build": "tsc -d",
    "pretest": "npm run build",
    "test": "nyc mocha ./dist/**/*.spec.js",
    "lint": "tslint src/**/*.ts"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/supergraphql/graphql-cli-bundle.git"
  },
  "keywords": [
    "graphql-cli"
  ],
  "author": "Kim Brandwijk <kim.brandwijk@gmail.com>",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/supergraphql/graphql-cli-bundle/issues"
  },
  "homepage": "https://github.com/supergraphql/graphql-cli-bundle#readme",
  "dependencies": {
    "chalk": "^2.3.0",
    "graphql-import": "^0.1.8"
  },
  "devDependencies": {
    "@types/node": "^8.5.1",
    "@types/mocha": "^2.2.44",
    "tslint": "^5.8.0",
    "typescript": "^2.6.2",
    "mocha": "^4.0.1",
    "mocha-junit-reporter": "^1.15.0",
    "nyc": "^11.3.0"
  }
}
